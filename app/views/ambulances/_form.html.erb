<%= form_for [@incident, @ambulance] do |ambulance| %>
 	<%= ambulance.label :idAmbulance, "Ambulance Number" %><br />
	<%= ambulance.text_field :idAmbulance %>
	<%= ambulance.select :status, [['En Route','En Route'],['Delayed','Delayed'],['Arrived','Arrived'],['Transported','Transported']] %>
	<%= collection_select(:ambulance, :hospital,Hospital.all ,:name, :name) %>
	<%= ambulance.label :eta, "ETA"%>
	<%= ambulance.text_field :eta %>
	<%= ambulance.submit "ADD"%>
<% end %> 
