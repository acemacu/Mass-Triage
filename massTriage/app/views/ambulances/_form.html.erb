<%= form_for(@ambulance) do |f| %>
  <% if @ambulance.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ambulance.errors.count, "error") %> prohibited this ambulance from being saved:</h2>

      <ul>
      <% @ambulance.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :idAmbulance %><br />
    <%= f.text_field :idAmbulance %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
